name: my_flutter_common
description: "A new Flutter package project."
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  my_flutter_util:
    path: ../my_flutter_util

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
